# Keep the Docker build context small and, crucially, never copy the host's
# platform-specific build artifacts into the Linux image (node_modules holds
# macOS/Windows native binaries that would break Electron inside the container).
node_modules
build
dist
release
out
test-output
.git
*.log
